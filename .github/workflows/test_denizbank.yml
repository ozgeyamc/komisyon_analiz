name: Test DenizBank URL

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Bagimlilik yukle
        run: |
          pip install playwright beautifulsoup4 lxml requests
          playwright install chromium

      - name: Test
        run: python test_denizbank.py
